# WebSocket Chat Service

Реалтайм-чат с поддержкой множества чатов, аутентификацией и сохранением истории сообщений в базе данных.

![Go](https://img.shields.io/badge/Go-1.22+-00ADD8?logo=go)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-14%2B-336791?logo=postgresql)
![WebSocket](https://img.shields.io/badge/WebSocket-RFC6455-20232A?logo=websocket)

## ✨ Возможности

- **Многопользовательский чат в реальном времени** через WebSocket
- **Поддержка множества чатов** — пользователи могут подключаться к разным чатам по ID
- **Сохранение истории сообщений** в PostgreSQL с автоматической подгрузкой при подключении
- **Аутентификация через JWT** — все операции защищены токенами
- **Масштабируемая архитектура**:
  - Чистая архитектура с разделением на слои (handlers → services → repositories)
  - Dependency Injection через централизованный контейнер приложения
  - Интерфейсы для репозиториев (легкая замена реализаций)
- **Надёжность и безопасность**:
  - Graceful shutdown для корректного завершения работы
  - Пул соединений с синхронизацией через `sync.Mutex` (защита от data race)
  - Буферизованные каналы для эффективной рассылки сообщений через горутины
- **Удобная разработка**:
  - Миграции базы данных через `goose`
  - Структурированное логирование
  - Маршрутизация через `chi`

## 🛠 Технологический стек

| Компонент       | Технология                 |
|----------------|----------------------------|
| Язык           | Go 1.22+                   |
| Веб-фреймворк  | `net/http` + `chi`         |
| База данных    | PostgreSQL + GORM          |
| Миграции       | `pressly/goose`            |
| Аутентификация | JWT (RS256)                |
| Логирование    | `log/slog`                 |
| Тестирование   | `testing`, `testify`       |
| Сборка         | `go build`                 |

## 🚀 Быстрый старт

### Требования
- Go 1.22+
- PostgreSQL 14+
- `goose` (для миграций): `go install github.com/pressly/goose/v3@latest`
